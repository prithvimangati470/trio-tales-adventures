{
  "name": "psa-diaries",
  "version": "1.0.0",
  "description": "A family travel landing page for PSA Diaries.",
  "private": true,
  "scripts": {
    "validate": "npx html-validator-cli --file=index.html --verbose",
    "accessibility": "npx pa11y-ci --config .pa11yci.json"
  },
  "devDependencies": {
    "html-validator-cli": "^7.0.1",
    "pa11y-ci": "^4.1.1"
  }
}
